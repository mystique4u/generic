# docker/

Compose examples and container-related assets for local development.

- `compose.example.yml` — placeholder `app` + Mongo + MySQL
- Prefer host tools for day-to-day coding; use Compose for dependencies

See [docs/docker-microservices.md](../docs/docker-microservices.md).
